---
import type { Locale } from '../i18n/translations';
import { t } from '../i18n/translations';

interface Props {
  locale?: Locale;
}

const locale = Astro.props.locale ?? 'en';
const i = t(locale);
---

<section class="hero">
  <div class="hero-inner">
    <div class="hero-badge">{i.hero.badge}</div>
    <h1 class="hero-title">
      {i.hero.title}<br />{i.hero.titleBreak}
    </h1>
    <p class="hero-desc">{i.hero.desc}</p>
    <div class="hero-actions">
      <a href="#install" class="btn btn-primary">{i.hero.cta}</a>
      <a href="#playground" class="btn btn-secondary">{i.hero.ctaSecondary}</a>
    </div>
    <div class="hero-terminal">
      <div class="terminal-bar">
        <span class="terminal-dot"></span>
        <span class="terminal-dot"></span>
        <span class="terminal-dot"></span>
        <span class="terminal-title">{i.hero.terminalTitle}</span>
      </div>
      <pre class="terminal-body"><code><span class="t-comment">{i.hero.terminalComment1}</span>

<span class="t-pass">PASS</span>  secret-scan         No secrets detected
<span class="t-pass">PASS</span>  sql-injection       No injection patterns found
<span class="t-warn">WARN</span>  pr-size-check       PR is Large (487 lines)
<span class="t-warn">WARN</span>  complexity-check    2 files exceed nesting depth 4
<span class="t-crit">CRIT</span>  xss-check           dangerouslySetInnerHTML in UserCard.tsx:42
<span class="t-pass">PASS</span>  lockfile-check      package-lock.json in sync
<span class="t-info">INFO</span>  todo-fixme-check    3 new TODO markers added
<span class="t-pass">PASS</span>  encoding-check      All files UTF-8, consistent line endings

<span class="t-comment">{i.hero.terminalComment2}</span></code></pre>
    </div>
  </div>
</section>

<style>
  .hero {
    padding: 80px 24px 64px;
    text-align: center;
  }

  .hero-inner {
    max-width: 720px;
    margin: 0 auto;
  }

  .hero-badge {
    display: inline-block;
    font-size: 0.78rem;
    font-weight: 500;
    color: var(--accent);
    background: var(--accent-light);
    padding: 5px 14px;
    border-radius: 20px;
    letter-spacing: 0.02em;
    margin-bottom: 24px;
  }

  .hero-title {
    font-family: 'DM Sans', var(--font-sans);
    font-weight: 700;
    font-size: clamp(1.8rem, 5vw, 2.6rem);
    line-height: 1.2;
    letter-spacing: -0.03em;
    text-wrap: balance;
    margin-bottom: 20px;
    color: var(--text);
  }

  .hero-desc {
    font-size: 1.05rem;
    color: var(--text-muted);
    line-height: 1.7;
    max-width: 560px;
    margin: 0 auto 32px;
  }

  .hero-actions {
    display: flex;
    justify-content: center;
    gap: 12px;
    margin-bottom: 48px;
    flex-wrap: wrap;
  }

  .btn {
    display: inline-flex;
    align-items: center;
    padding: 10px 24px;
    border-radius: 6px;
    font-size: 0.9rem;
    font-weight: 600;
    text-decoration: none;
    transition: background 0.15s, color 0.15s, box-shadow 0.15s;
  }

  .btn-primary {
    background: var(--accent);
    color: #fff;
  }

  .btn-primary:hover {
    background: var(--accent-hover);
    text-decoration: none;
  }

  .btn-secondary {
    background: var(--surface);
    color: var(--text);
    border: 1px solid var(--border);
  }

  .btn-secondary:hover {
    background: var(--border-light);
    text-decoration: none;
  }

  .hero-terminal {
    background: var(--bg-elevated);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    overflow: hidden;
    text-align: left;
    box-shadow: var(--shadow-md);
  }

  .terminal-bar {
    display: flex;
    align-items: center;
    gap: 6px;
    padding: 10px 14px;
    background: var(--surface);
    border-bottom: 1px solid var(--border-light);
  }

  .terminal-dot {
    width: 10px;
    height: 10px;
    border-radius: 50%;
    background: var(--border);
  }

  .terminal-title {
    font-family: var(--font-mono);
    font-size: 0.75rem;
    color: var(--text-faint);
    margin-left: 8px;
  }

  .terminal-body {
    padding: 18px 20px;
    overflow-x: auto;
    font-family: var(--font-mono);
    font-size: 0.8rem;
    line-height: 1.8;
    color: var(--text-muted);
    margin: 0;
  }

  .terminal-body code {
    background: none;
    padding: 0;
    font-size: inherit;
  }

  .t-comment { color: var(--text-faint); }
  .t-pass { color: var(--pass); font-weight: 500; }
  .t-warn { color: var(--warn); font-weight: 500; }
  .t-crit { color: var(--critical); font-weight: 500; }
  .t-info { color: var(--info); font-weight: 500; }

  @media (max-width: 600px) {
    .hero {
      padding: 48px 20px 40px;
    }
    .terminal-body {
      font-size: 0.72rem;
      padding: 14px 12px;
    }
  }
</style>
